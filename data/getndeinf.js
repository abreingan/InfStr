function gni () {
/*
function getndeinf (node data) {
	console.log("hellogni");
	var sch = "*LAST";
	var schsub = " ";
	var fldsel = "";
	if (data == "*YER") {
		if (( node.tagName.toLowerCase() == "select" ) && (node.options[1].value.length > 2 )) {
				fldsel = "ExpDte;Y4";
		} else {
				fldsel = "ExpDte;Y";
			}
	} else if (data == "*MTH") {
		if (( node.tagName.toLowerCase() == "select" ) && (node.options[1].value.length > 2 )) {
				fldsel = "ExpDte;M3";
		} else {
				fldsel = "ExpDte;M";
			}
	} else if (data == "*VFY") {
		fldsel = "CrdVrf";
	} else {
		sch = data;
		schsub = "*CurUsr";
		fldsel = "CrdNbr";
	}
	return [node.id, node.name, sch, schsub, fldsel];
*/
return "helo";	
};
